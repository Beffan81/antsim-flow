// Update this page (the content is just a fallback if you fail to update the page)

import AntsimApp from '@/components/antsim-app';

const Index = () => {
  return <AntsimApp />;
};

export default Index;
